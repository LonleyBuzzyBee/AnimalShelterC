 @{
   Layout = "_Layout";
 }
 
 @using AnimalShelter.Models;

  <h1>Animals</h1>
    @if (Model.Count == 0)
    {
      <p>Nothing here bro.</p>
    }
    <ul>
      @foreach (Animal animal in Model)
      {
      <li>@item.Description</li>
      }
    </ul>
